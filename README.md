# EKS_cluster_ALB_controller
Using Terraform modules to setup eks cluster with alb controller

```
# terraform init
# terraform plan
# terraform apply -auto-approve
# aws eks update-kubeconfig --name my-eks --region us-east-1
```